import styled from "@emotion/styled";

export default function LayoutFootr(): JSX.Element {
  const Wrapper = styled.div`
    height: 50px;
    background-color: #4a7c31;
  `;

  return <Wrapper>푸터 영역</Wrapper>;
}
