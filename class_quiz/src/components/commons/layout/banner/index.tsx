import styled from "@emotion/styled";

export default function LayoutBanner() {
  const Wrapper = styled.div`
    height: 150px;
    background-color: #eabbc0;
  `;

  return <Wrapper>배너 영역</Wrapper>;
}
